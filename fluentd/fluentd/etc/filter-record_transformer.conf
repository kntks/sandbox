<source>
  @type http
  port 24224
</source>

# https://docs.fluentd.org/filter/record_transformer
<filter **>
  @type record_transformer
  remove_keys name,age
</filter>

<match **>
  @type stdout
</match>
